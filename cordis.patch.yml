# dsh-usage-dashboard bundle patch: mount the host plugin under its package
# name, resolved through the profile's node_modules.
- insert:
    - id: dsh-usage-dashboard
      name: dsh-usage-dashboard
